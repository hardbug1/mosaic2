{
  "name": "mosaic2",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:push": "set -a; . ./.env.local; set +a; npx supabase db push --db-url \"$SUPABASE_DB_URL\"",
    "db:migrations": "set -a; . ./.env.local; set +a; npx supabase migration list --db-url \"$SUPABASE_DB_URL\""
  },
  "dependencies": {
    "@supabase/ssr": "^0.5.2",
    "@supabase/supabase-js": "^2.106.2",
    "next": "16.2.6",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "dotenv": "^17.4.2",
    "typescript": "^5",
    "vitest": "^4.1.8"
  }
}
